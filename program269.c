#include<stdio.h>
#include<stdlib.h>

typedef struct Node {
    struct Node * prev;   //#
    int data ;
    struct node * next;
}NODE , *PNODE , **PPNODE;

int main()
{
    PNODE First = NULL:

    return 0;
}